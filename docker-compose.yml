version: "3.9"
services:
  frontend:
    build: .
    container_name: patient-frontend
    ports:
      - "10080:80"           # host:container
    environment:
      API_BASE: "http://localhost:10081"   # your backend’s local host:port
    restart: unless-stopped
